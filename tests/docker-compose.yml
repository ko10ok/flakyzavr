services:
  e2e:
    build:
      context: .
      dockerfile: Dockerfile
    command: 'sh -c "trap : TERM INT; sleep 604800; wait"'
    environment:
      - JJ_REMOTE_MOCK_URL=http://mock:80
    volumes:
      - .:/tests
      - ..:/flakyzavr

  mock:
    image: vedrouniverse/jj:2.10.0
    ports:
      - "80"
